# Binaries and executables
# (Add the project's compiled binary name if custom)
/post-article-be
/server
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built by `go test`
*.test

# Output of the go coverage tool, generated by `go test -coverprofile`
*.out

# Dependency directories (only needed if you keep local copies of dependencies)
# vendor/

# Go workspace file
go.work
go.work.sum

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDEs / Editors
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS files
.DS_Store
Thumbs.db